#wpt find all alnum in given string
s = 'kis12hore'
c =0
for i in s:
    if i.isalnum():
        c+=1
print(c)
#iteration
'''
1. getting string as a input
2. using for loop for iterating each element
    #checking that 'k' is alpha numeric

    #checking that 'i' is alpha numeric

    #checking that 's' is alpha numeric

    #checking that '1' is alpha numeric

    #checking that '2' is alpha numeric

    #checking that 'h' is alpha numeric

    #checking that '0' is alpha numeric

    #checking that 'r' is alpha numeric

    #checking that 'e' is alpha numeric


4. if it is true than c increment by 1.

'''